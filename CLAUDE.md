# Claude Code instructions

This project's agent guidance lives in [`AGENTS.md`](AGENTS.md).

Claude Code loads `CLAUDE.md` by default. Read that file instead of
duplicating it here. Do not copy `AGENTS.md` into this file.

Depth (CI, Pages, releases, implementation notes, the full tree) lives in
[`docs/agent/`](docs/agent/) and is loaded only when the task needs it —
see the routing table in `AGENTS.md`.
