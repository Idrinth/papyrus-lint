# papyrus-parser

Lexer, parser, AST, visitors, comment annotations, and type inference.

Start at `src/ast.rs`, `src/parser.rs`, `src/lexer.rs`, `src/cache.rs`, and
`src/types.rs`.

```sh
cargo test --manifest-path app/crates/papyrus-parser/Cargo.toml
```
